# ===================================================================== #
# LLMCL-Rumor 运行环境
# --------------------------------------------------------------------- #
# 本文件只列"训练与增强"所需的依赖。原 BERT 基线使用的
# pytorch_pretrained_bert 已停止维护且不支持新版 PyTorch/CUDA，
# 因此这里统一改用 transformers（详见 docs/differences_from_baseline.md）。
#
# 安装（建议 CUDA 12.1 环境，torch 单独按官网命令安装）：
#   conda create -n llmcl python=3.10 -y && conda activate llmcl
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# ===================================================================== #

# ---- 深度学习基础 ----
torch>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0

# ---- 句向量编码器（CL 网络的 BERT 特征提取器）----
transformers>=4.40.0
tokenizers>=0.19.0
sentencepiece>=0.1.99

# ---- LLM 数据增强与 LoRA 微调 ----
accelerate>=0.30.0
peft>=0.10.0
bitsandbytes>=0.43.0      # 可选：13B 单卡 8bit 加载
datasets>=2.19.0

# ---- 配置与数据 ----
pyyaml>=6.0
tqdm>=4.66.0
pandas>=2.1.0

# ---- LLM API 后端（backend=api，可选）----
requests>=2.31.0

# ---- 评估与可视化 ----
matplotlib>=3.8.0
tensorboard>=2.15.0

# ---- 测试（仅纯 CPU 单测，不需要模型权重）----
pytest>=7.4.0
